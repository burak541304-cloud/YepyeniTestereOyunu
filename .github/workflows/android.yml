name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
      
      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Node Install
        run: npm install

      - name: Web Build
        run: npm run build

      - name: Capacitor Setup & Duplicate Fix
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap init "Testere" "com.burak.testere" --web-dir dist
          npx cap add android
          
          # 1. DUPLICATE CLASS FIX: Çakışan Kotlin kütüphanelerini devre dışı bırak
          echo "android.useAndroidX=true" >> android/gradle.properties
          echo "android.enableJetifier=true" >> android/gradle.properties
          echo "kotlin.stdlib.default.dependency=false" >> android/gradle.properties

          # 2. VERSION FIX: Java 21 zorlaması
          find android -name "build.gradle" -exec sed -i 's/JavaVersion.VERSION_17/JavaVersion.VERSION_21/g' {} +
          find android -name "build.gradle" -exec sed -i 's/JavaVersion.VERSION_1_8/JavaVersion.VERSION_21/g' {} +
          
          npx cap sync android

      - name: Build APK File
        run: |
          cd android
          chmod +x gradlew
          # Çakışmaları görmezden gelmesi için 'exclude' komutuyla derle
          ./gradlew assembleDebug --no-daemon -Pkotlin.version=1.8.22

      - name: Save APK
        uses: actions/upload-artifact@v4
        with:
          name: oyun-apk-dosyasi
          path: android/app/build/outputs/apk/debug/app-debug.apk
          
